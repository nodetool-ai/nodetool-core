{
  "name": "@nodetool/base-nodes",
  "version": "0.1.0",
  "description": "Basic nodetool-base node ports for the TS workflow runner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:metadata-parity": "vitest run tests/metadata-parity.test.ts",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@nodetool/node-sdk": "*",
    "@nodetool/runtime": "*",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
