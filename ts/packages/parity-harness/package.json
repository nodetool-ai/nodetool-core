{
  "name": "@nodetool/parity-harness",
  "version": "0.1.0",
  "description": "Dual-run shadow and diff harness for workflow parity",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "nodetool-shadow-report": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
