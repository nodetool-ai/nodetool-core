{
  "name": "@nodetool/websocket",
  "version": "0.1.0",
  "type": "module",
  "description": "Unified websocket runner for workflow and chat operations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@nodetool/kernel": "*",
    "@nodetool/models": "*",
    "@nodetool/protocol": "*",
    "@nodetool/runtime": "*",
    "msgpackr": "^1.11.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
